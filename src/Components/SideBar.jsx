import React from 'react';

const SideBar = () => {
  return (
    <div className="sidebar">
      <button>Home</button>
      <button>About</button>
      <button>Contact</button>
    </div>
  );
};

export default SideBar;

